const SearchBar = () => {
  return (
    <input
      type="text"
      placeholder="Search lecture hall, department, building…"
      className="input text-sm"
    />
  );
};

export default SearchBar;
